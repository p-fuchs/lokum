services:
  postgres:
    image: postgres:17
    environment:
      POSTGRES_USER: lokum
      POSTGRES_PASSWORD: lokum
      POSTGRES_DB: lokum
    ports:
      - "5432:5432"
    volumes:
      - pgdata:/var/lib/postgresql/data

volumes:
  pgdata:
